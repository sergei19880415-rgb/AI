"use client";

import Layout from "@/components/Login/Layout";
import Start from "./Start";

const SignUpPage = () => {
    return (
        <Layout>
            <Start />
        </Layout>
    );
};

export default SignUpPage;
